public class FaultlyKeyboard {
    public static void main(String[] args) {

    }

    public String finalString(String s) {
        StringBuilder sb = new StringBuilder(s);
        StringBuilder sol = new StringBuilder();
        int n = s.length();
        int i = 0;
        int j = 0;
        while (j < n) {
            if (sb.charAt(j) == 'i') {
                int len = (j - i);
                while (i < len / 2) {
                    sol+=
                }
            }

        }

    }

}