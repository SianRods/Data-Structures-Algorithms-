    // Questions --> removeDuplicates
    // note that here at the end of the list we will directly update the temp.next==null
    // hence we have to just run the loop till temp.next != null
    // Note that we have to also consider the affect of changes in the main linkedList to the other parameters 
    // size variable changes 

    // void removeDuplicates(){
    //     Node temp = head ;
    //     while(temp.next!=null){
    //         if(temp.data==temp.next.data){
    //             temp.next=temp.next.next;
    //             size--;
    //         }else{
    //             temp=temp.next;
    //         }
    //     }
    //     tail=temp;
    //     tail.next=null;

        // updating the tail to the current temp node ;

    }
